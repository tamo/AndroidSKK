<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- おもに声入力の候補が複数のとき使うテーマなので dialog -->
    <style name="Theme.SKK" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="keyboardViewStyle">@style/KeyboardView</item>
    </style>

    <style name="KeyboardView" parent="android:Widget">
        <item name="android:background">#FFAAAAAA</item>
        <item name="keyTextSize">28sp</item>
        <item name="keyTextColor">@color/key_char_color</item>
        <item name="keyBackground">@drawable/key_bg</item>
        <item name="keyPreviewOffset">-12dp</item>
        <item name="keyPreviewHeight">80dp</item>
        <item name="keyPreviewLayout">@layout/keyboard_key_preview</item>
        <item name="labelTextSize">18sp</item>
        <item name="popupLayout">@layout/keyboard_popup_keyboard</item>
        <item name="verticalCorrection">-10dp</item>
    </style>
</resources>